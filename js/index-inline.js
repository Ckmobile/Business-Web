(function(){var input=document.getElementById('searchInput');var btn=document.getElementById('searchBtn');if(!input||!btn)return;function syncButton(){if(input.value.trim().length>0){btn.innerHTML='<i class="fas fa-times"></i>';btn.setAttribute('aria-label','Clear search');}else{btn.textContent='Search';btn.removeAttribute('aria-label');}}input.addEventListener('input',syncButton);btn.addEventListener('click',function(){if(input.value.trim().length>0){input.value='';syncButton();input.dispatchEvent(new Event('input',{bubbles:true}));input.focus();}});syncButton();})();